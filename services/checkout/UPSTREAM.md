# Source provenance

This service was imported from the former MyAvenCEO monolith, subdirectory
`aven-minimal`, at commit `6aba9f1` on 20 August 2026.

The source was copied from Git-tracked files only. Changes after the import are
native to the avenOS monorepo; there is no permanent subtree remote or automatic
upstream synchronization.
