{
  "typeKey": "core.file",
  "version": 1,
  "schemaProfileId": "artifact-json-schema-profile-v1",
  "payloadSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "additionalProperties": false,
    "required": ["originalName", "declaredMediaType", "sourceKind"],
    "properties": {
      "originalName": { "type": "string", "minLength": 1, "maxLength": 512 },
      "declaredMediaType": { "type": "string", "minLength": 1, "maxLength": 255 },
      "sourceKind": { "type": "string", "minLength": 1, "maxLength": 64 },
      "executionEnvironment": { "type": "string", "enum": ["local", "server"] }
    }
  },
  "blobPolicy": "required",
  "referenceRules": []
}
